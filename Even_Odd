const prompt = require('prompt-sync')();
let num= prompt("enter a valid number to check whether its even or odd");
if(num%2==0)
{
    console.log(`${num} is an even number`);
}
else{
    console.log(`${num} is an odd number`);
}
